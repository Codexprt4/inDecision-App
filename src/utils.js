console.log("Util Is Running"); 

export const square = (x) => x * x;
export const add = (a, b) => a + b;
// const subtract = (a, b) => a - b;
// export default subtract; // inline export default
export default (a, b) => a - b; // can also write without taking it in variable, can give any name for import


//export - default - named 

//named
//export {square, add};  //export with keyword

